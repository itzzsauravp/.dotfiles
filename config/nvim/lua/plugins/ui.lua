require("mini.animate").setup({
  scroll = {
    enable = true,
    timing = require("mini.animate").gen_timing.linear({ duration = 0 }),
  },
  resize = {
    enable = true,
    timing = require("mini.animate").gen_timing.linear({ duration = 0 }),
  },
  cursor = {
    enable = true,
    timing = require("mini.animate").gen_timing.linear({ duration = 0 }),
  },
  open = {
    enable = true,
    timing = require("mini.animate").gen_timing.linear({ duration = 0 }),
  },
  close = {
    enable = true,
    timing = require("mini.animate").gen_timing.linear({ duration = 0 }),
  },
})
